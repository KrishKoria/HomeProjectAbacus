resources:
  pipelines:
    gold_pipeline:
      name: "[${bundle.target}] Healthcare Gold Features"
      catalog: ${var.catalog}
      target: ${var.gold_schema}
      serverless: true
      channel: current
      libraries:
        - glob:
            include: ../../../../ETL/pipelines/gold/**
        - glob:
            include: ../../../../ETL/common/**
        - glob:
            include: ../../../../src/common/**
      event_log:
        catalog: ${var.catalog}
        schema: ${var.analytics_schema}
        name: gold_pipeline_event_log
