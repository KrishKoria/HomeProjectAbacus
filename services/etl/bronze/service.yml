service:
  name: etl_bronze
  type: pipeline
  version: "1.0.0"
  description: Bronze Lakeflow ingestion pipeline.
entry_point:
  resource_key: bronze_pipeline
  resource_type: pipelines
health_check:
  type: script
  entry_point: src/scripts/verify_bronze.py
